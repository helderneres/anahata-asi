Put all your findings from this session here, update it on every turn

The goal is to be able to use the NbEditorUI with an in memory or tmp dir based FileObject for activating syntax highlighting on snippets such as 

Markup codeblcks in the chat (global classpath)
compileAndExecute (classpath from the java tool + extraClasspath)
compileAndExecuteInProject (classpath from the java tool + calculated extra classpath from NbJava (project and possibly project deps) + extraClasspath)
createTextFile (project classpath where the file will be created)

As well as normal java project files (for the context panel resource viewer) - less priority but also needed

### Discoveries & Solutions

1. **The Classpath Loss Issue**:
   When opening an in-memory `FileObject` (e.g., `mem:///demo/DemoSnippet.java`), the `JavacParser` attempts to resolve its classpath. It delegates to `ClassPath.getClassPath(fo, type)`, which queries all `ClassPathProvider`s in the global `Lookup`. Since the memory file doesn't belong to a NetBeans Project, all providers return `null`. The parser falls back to an empty classpath, preventing `SemanticHighlighter` from resolving symbols like `List` or `JButton`.

2. **The `boot.jar` Error**:
   When manually constructing a `ClassPath` using `ClassPathSupport.createClassPath(URL[])`, standard file URLs (e.g., `file:/path/to/my.jar`) are rejected for archives. NetBeans requires the `jar:` protocol. We must wrap `File` objects with `FileUtil.urlForArchiveOrDir(file)` to generate compliant URLs.
   **Why it's not the same as bootstrap libraries:** `boot.jar` from the NetBeans toolkit provides the NetBeans module system bootstrapper. It is part of the application's runtime but *not* part of the JDK's standard library. `JavaPlatformManager.getDefault().getDefaultPlatform().getBootstrapLibraries()` specifically targets the JDK classes (`java.lang.*`, etc.).

3. **The JEditorPane `setText()` Trap**:
   Calling `editor.setText(code)` completely discards the existing `Document` and instantiates a new one. If we inject metadata (like `StreamDescriptionProperty` or `JavaSource.class`) *before* setting the text, the properties are lost on the discarded document. The correct sequence is:
   1. `Document doc = editor.getDocument();`
   2. `doc.remove(0, doc.getLength());`
   3. `doc.insertString(0, newText, null);`
   This preserves the original `NbEditorDocument` and its crucial `DataObject` bindings. This was the root cause for **normal project files** failing to show highlighting in the Context Panel!

4. **PathHandle Resolution Bug**:
   `NetBeansTextResourceViewer` was completely ignoring `PathHandle`, failing to resolve a `FileObject` for physical files generated by the `createTextFile` tool. This caused them to fall back to `text/plain` with no `DataObject` binding, which explains why the Popup Demo failed for project files.

5. **Dynamic Global Providers via SystemFileSystem (The Elegant Fix)**:
   NetBeans allows dynamic or declarative registration of `ClassPathProvider`s. We created `AnahataClasspathProvider` with `@ServiceProvider(service = ClassPathProvider.class, position = 10000)`. It detects if a `FileObject` comes from the `MemoryFileSystem` and responds with the `NbJava.getDefaultClasspath()`. This seamlessly integrates with the background parser without any reflection or document-property hacks.
   *Note 1:* `org.openide.filesystems.MemoryFileSystem` is package-private, so we must use `.getClass().getSimpleName().equals("MemoryFileSystem")` instead of `instanceof` to avoid compilation errors.
   *Note 2:* Position 10000 guarantees our provider acts as a fallback *after* standard project providers. This prevents it from overriding valid project file classpath resolutions. Logging inside `findClassPath` must be strictly avoided to prevent flooding the IDE's parsing loop.

6. **The Boot Classpath and JavaPlatformManager**:
   If we return `null` for `ClassPath.BOOT` in our custom `ClassPathProvider`, `JavaSource` automatically falls back to `JavaPlatformManager.getDefault().getDefaultPlatform().getBootstrapLibraries()`. This elegantly solves the issue of providing standard JDK classes (like `java.lang.String`) without manually resolving the boot classpath.

7. **Custom Attributes for ClassPath Context Targeting**:
   Since all virtual resources are created via `StringHandle` which maps to `MemoryFileSystem`, we can set custom attributes on the `FileObject` when it is created (e.g., `fo.setAttribute("anahataClasspathContext", "global")` or `fo.setAttribute("anahataClasspathContext", "/path/to/project")`). Our dynamic `ClassPathProvider` can then read this attribute and decide *which* `ClassPath` to return, enabling distinct classpaths for global chat snippets vs. project-bound `createTextFile` operations!

8. **Missing Error Highlighting (Hints / Annotations)**:
   Semantic highlighting (types, variables) works via `SemanticHighlighter`, but error checking (red squiggles) is usually provided by `HintsTask` / `HintsInvoker` which are executed asynchronously. If errors are not showing up in our embedded viewer, it may be because `HintsTask` relies on a fully realized `CloneableEditor` or checks `OpenProjects` to decide whether to run. We will investigate this next.

### Discoveries & Solutions (Part 3)

11. **The EditorCookie Magic**:
    The true native way to bind a `FileObject` to a NetBeans `JEditorPane` and achieve 100% IDE fidelity (including error squiggles, code folds, and classpath resolution) is by using the `EditorCookie`. Instead of manually setting the content type, creating a default document, and injecting properties via reflection, we simply:
    1. Look up the `DataObject` for the `FileObject`.
    2. Obtain the `EditorCookie` from its lookup.
    3. Call `openDocument()` on the cookie.
    4. Set this document to the `JEditorPane`, ensuring the `EditorKit` matches.
    This guarantees that the NetBeans infrastructure fully adopts the editor component, treating it exactly like a natively opened file.
    
12. **Virtual Snippet Classpath Targeting**:
    For memory-backed files (like those created via `createTextFile`), we write the content to an ephemeral `MemoryFileSystem` file. To provide the correct project classpath (so the parser understands project-specific imports), we introduce `StringHandle.setContextPath()`. 
    When the virtual file is created, we inject this path as a custom attribute: `finalFo.setAttribute("anahata.contextPath", sh.getContextPath())`. 
    Our global `AnahataClasspathProvider` intercepts the `findClassPath` call for the memory file, reads the attribute, looks up the owning `Project` via `FileOwnerQuery`, and delegates to the project's native `ClassPathProvider`.

13. **What is the `SOURCE` Classpath?**:
    The `ClassPath.SOURCE` represents the root directories containing source files (e.g., `src/main/java`). NetBeans uses this to locate other source files in the same project, enabling features like cross-referencing, "Go to Declaration" for uncompiled files, and global refactoring scoping.

### Relevant Sources Examined

**NetBeans Classes:**
- `org.netbeans.api.java.source.JavaSource`
- `org.netbeans.modules.parsing.api.Source`
- `org.netbeans.modules.parsing.api.ParserManager`
- `org.netbeans.modules.parsing.impl.TaskProcessor`
- `org.netbeans.modules.java.source.parsing.JavacParser`
- `org.netbeans.editor.EditorUI`
- `org.netbeans.modules.editor.NbEditorUI`
- `org.netbeans.modules.editor.NbEditorDocument`
- `org.netbeans.modules.editor.NbEditorUtilities`
- `org.netbeans.modules.editor.NbEditorToolBar`
- `org.netbeans.modules.editor.impl.NbEditorImplementationProvider`
- `org.openide.loaders.DataObject`
- `org.openide.filesystems.FileObject`
- `org.openide.text.CloneableEditorSupport`
- `org.netbeans.modules.project.ui.OpenProjectList`
- `org.netbeans.modules.java.editor.semantic.SemanticHighlighter`
- `org.netbeans.modules.java.editor.semantic.LexerBasedHighlightLayer`
- `org.netbeans.spi.editor.highlighting.HighlightsLayer`
- `org.netbeans.modules.editor.lib2.highlighting.HighlightingManager`
- `org.netbeans.spi.editor.highlighting.support.OffsetsBag`
- `org.netbeans.api.java.classpath.ClassPath`
- `org.netbeans.api.java.classpath.GlobalPathRegistry`
- `org.netbeans.spi.java.classpath.ClassPathProvider`
- `org.netbeans.modules.java.source.usages.ClasspathInfoAccessor`
- `org.netbeans.modules.java.classpath.ClassPathAccessor`
- `org.netbeans.spi.java.classpath.ClassPathImplementation`
- `org.netbeans.spi.java.classpath.PathResourceImplementation`
- `org.netbeans.spi.java.classpath.support.ClassPathSupport`
- `org.netbeans.spi.java.classpath.FilteringPathResourceImplementation`
- `org.netbeans.spi.java.classpath.FlaggedClassPathImplementation`
- `org.openide.filesystems.MemoryFileSystem`
- `org.netbeans.modules.java.source.parsing.FileObjects`
- `org.netbeans.modules.java.source.parsing.MemoryFileManager`
- `org.netbeans.modules.parsing.api.Task`
- `org.netbeans.modules.java.editor.semantic.SemanticHighlighterFactory`
- `org.netbeans.spi.editor.highlighting.ZOrder`
- `org.netbeans.modules.editor.lib2.highlighting.HighlightingSpiPackageAccessor`
- `org.netbeans.modules.editor.lib2.highlighting.HighlightsLayerAccessor`
- `org.netbeans.modules.editor.lib2.highlighting.DirectMergeContainer`
- `org.netbeans.modules.editor.java.JavaKit`
- `org.netbeans.api.editor.EditorRegistry`
- `org.netbeans.modules.editor.lib2.highlighting.HighlightingSpiPackageAccessor`
- `org.netbeans.spi.debugger.ui.EditorContextDispatcher$EditorRegistryListener`
- `org.netbeans.modules.editor.lib2.actions.EditorRegistryWatcher`

**Anahata ASI Classes:**
- `uno.anahata.asi.nb.tools.java.classpath.AnahataClasspathProvider`
- `uno.anahata.asi.swing.agi.message.part.text.AbstractTextSegmentRenderer`
- `uno.anahata.asi.swing.agi.message.part.text.CodeBlockSegmentRenderer`
- `uno.anahata.asi.swing.agi.message.part.text.MermaidCodeBlockSegmentRenderer`
- `uno.anahata.asi.swing.agi.message.part.text.MarkupTextSegmentRenderer`
- `uno.anahata.asi.nb.ui.resources.NetBeansTextResourceViewer`
- `uno.anahata.asi.swing.agi.resources.view.AbstractTextResourceViewer`
- `uno.anahata.asi.swing.agi.resources.view.RSyntaxTextAreaTextResourceViewer`
- `uno.anahata.asi.swing.agi.resources.ResourceUI`
- `uno.anahata.asi.nb.ui.resources.NbResourceUI`
- `uno.anahata.asi.swing.agi.resources.DefaultResourceUI`
- `uno.anahata.asi.nb.tools.java.CodeModel`
- `uno.anahata.asi.nb.tools.java.NbJava`
- `uno.anahata.asi.toolkit.java.Java`
- `uno.anahata.asi.swing.toolkit.SwingJava`
- `uno.anahata.asi.nb.module.NetBeansModuleUtils`
- `uno.anahata.asi.swing.agi.message.part.tool.param.FullTextFileCreateRenderer`
- `uno.anahata.asi.swing.agi.context.ContextPanel`
- `uno.anahata.asi.swing.agi.context.AbstractContextNode`
- `uno.anahata.asi.swing.agi.context.ContextManagerNode`
- `uno.anahata.asi.swing.agi.context.ContextProviderPanel`
- `uno.anahata.asi.swing.agi.context.ContextTreeCellRenderer`
- `uno.anahata.asi.swing.agi.context.ContextTreeTableModel`
- `uno.anahata.asi.swing.agi.context.HistoryNode`
- `uno.anahata.asi.swing.agi.context.MessageNode`
- `uno.anahata.asi.swing.agi.context.PartNode`
- `uno.anahata.asi.swing.agi.context.ProviderNode`
- `uno.anahata.asi.swing.agi.context.ToolNode`
- `uno.anahata.asi.swing.agi.context.ToolPanel`
- `uno.anahata.asi.swing.agi.context.ToolkitNode`
- `uno.anahata.asi.swing.agi.context.ToolkitPanel`
- `uno.anahata.asi.swing.agi.context.ToolsNode`
- `uno.anahata.asi.agi.resource.ResourceManager`
- `uno.anahata.asi.agi.resource.Resource`
- `uno.anahata.asi.agi.resource.RefreshPolicy`
- `uno.anahata.asi.agi.resource.handle.UrlHandle`
- `uno.anahata.asi.agi.resource.handle.ResourceHandle`
- `uno.anahata.asi.agi.resource.handle.PathHandle`
- `uno.anahata.asi.agi.resource.handle.AbstractResourceHandle`
- `uno.anahata.asi.agi.resource.handle.StringHandle`
- `uno.anahata.asi.agi.resource.view.ResourceView`
- `uno.anahata.asi.agi.resource.view.TextView`
- `uno.anahata.asi.agi.resource.view.MediaView`
- `uno.anahata.asi.agi.resource.view.TextViewport`
- `uno.anahata.asi.agi.resource.view.TextViewportSettings`
- `uno.anahata.asi.agi.resource.view.AbstractResourceView`
- `uno.anahata.asi.swing.agi.resources.DefaultResourceUI`
- `uno.anahata.asi.swing.agi.resources.ResourceNode`
- `uno.anahata.asi.swing.agi.resources.ResourcePanel`
- `uno.anahata.asi.swing.agi.resources.ResourceUI`
- `uno.anahata.asi.swing.agi.resources.ResourceUiRegistry`
- `uno.anahata.asi.swing.agi.resources.ResourcesNode`
- `uno.anahata.asi.swing.agi.resources.handle.AbstractHandlePanel`
- `uno.anahata.asi.swing.agi.resources.handle.PathHandlePanel`
- `uno.anahata.asi.swing.agi.resources.handle.StringHandlePanel`
- `uno.anahata.asi.swing.agi.resources.handle.UrlHandlePanel`
- `uno.anahata.asi.swing.agi.resources.view.AbstractTextResourceViewer`
- `uno.anahata.asi.swing.agi.resources.view.AbstractViewPanel`
- `uno.anahata.asi.swing.agi.resources.view.MediaViewPanel`
- `uno.anahata.asi.swing.agi.resources.view.RSyntaxTextAreaTextResourceViewer`
- `uno.anahata.asi.swing.agi.resources.view.TextViewPanel`
- `uno.anahata.asi.swing.agi.status.SwingTaskMonitor`
- `uno.anahata.asi.swing.agi.status.TaskStatusComponent`
- `uno.anahata.asi.swing.internal.SwingUtils`
- `uno.anahata.asi.swing.internal.SwingTaskManager`
- `uno.anahata.asi.swing.internal.SwingTask`
- `uno.anahata.asi.nb.ui.resources.NbHandlePanel`
- `uno.anahata.asi.nb.ui.resources.NbResourceUI`
- `uno.anahata.asi.nb.ui.resources.NetBeansTextResourceViewer`
- `uno.anahata.asi.nb.ui.render.AbstractTextResourceWriteRenderer`
- `uno.anahata.asi.nb.ui.render.FullTextResourceUpdateRenderer`
- `uno.anahata.asi.nb.ui.render.TextResourceReplacementsRenderer`
- `uno.anahata.asi.nb.ui.render.CodeRefinementBatchRenderer`
- `uno.anahata.asi.nb.ui.render.DiffAnnotationsLayerUI`
- `uno.anahata.asi.nb.ui.render.DiffCommentUtils`
- `uno.anahata.asi.swing.agi.message.part.tool.param.FullTextFileCreateRenderer`
- `uno.anahata.asi.swing.agi.message.part.tool.param.MarkupParameterRenderer`
- `uno.anahata.asi.swing.agi.message.part.tool.param.ParameterRenderer`
- `uno.anahata.asi.swing.agi.message.part.tool.param.ParameterRendererFactory`
- `uno.anahata.asi.swing.agi.message.part.tool.param.ObjectToStringParameterRenderer`
- `uno.anahata.asi.swing.agi.message.AbstractMessagePanel`
- `uno.anahata.asi.swing.agi.message.GroundingMetadataPanel`
- `uno.anahata.asi.swing.agi.message.MessagePanelFactory`
- `uno.anahata.asi.swing.agi.message.ModelMessagePanel`
- `uno.anahata.asi.swing.agi.message.RagMessagePanel`
- `uno.anahata.asi.swing.agi.message.RagMessageViewer`
- `uno.anahata.asi.swing.agi.message.UserMessagePanel`
- `uno.anahata.asi.swing.agi.message.part.text.TextSegmentDescriptor`
- `uno.anahata.asi.swing.agi.message.part.text.TextSegmentType`
- `uno.anahata.asi.swing.agi.message.part.TextPartPanel`
- `uno.anahata.asi.swing.agi.message.part.tool.ToolCallPanel`